[x] 一个检查 dangling link 的命令 — `interproof check`（`--json` / `--strict`，非零退出码可进 CI）
[x] Make the site self-contained. — 产物是一个自包含 folder：index.html + pdf/ + sources/ + manifest.json + interproof.toml
[x] 转换为通用独立框架。完善说明。说明使用方式。添加 live site 和静态编译两种模式。
[x] Provide download command (the whole site including latex, pdf and code) — 右上角 Download 按钮，浏览器内打包 zip
[x] 引用结构
[ ] 优化界面设计和元素
[x] 全部高亮机制

[ ] 站内 `?` 帮助面板（快捷键、三种索引、引用协议摘要、dangling 报告）
[ ] 发布到 PyPI（`pipx install interproof`），仓库推到 GitHub
[ ] Proof-body ↔ tactic-block 对齐（真正的研究贡献，需要 SubVerso）
